import React from 'react'
import { Button } from '@chakra-ui/react'

function StartButton() {
  return (
    <Button backgroundColor={'green.700'} width={200} m={'auto'}>Start Game</Button>
  )
}

export default StartButton